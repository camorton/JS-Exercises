//not on test!
const submissions = [
//insert Jane, Joe, etc submissions from lab three
];

const submission = { name: 'Jane, score: 95, date: passed'}

function countElements(array) {
    var newObject = {};
    array.forEach(letter => {
        if (newObject.hasOwenProperty(letter)) {
            newObject[letter] += 1;
        } else; {
            newObject[letter] = 1;
        }
        })

    }